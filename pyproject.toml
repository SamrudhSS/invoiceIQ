[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "invoiceiq"
version = "0.1.0"
description = "Enterprise invoice processing system"

[tool.setuptools.packages.find]
where = ["src"]